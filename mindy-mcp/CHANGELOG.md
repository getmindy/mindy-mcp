# Changelog

All notable changes to the Mindy MCP developer surface. The server itself ships continuously; this log tracks catalog and docs changes developers should know about.

## [Unreleased]
- Developer docs repo initialized: quickstart, auth, tool catalog, per-client guides, examples.

## Catalog
- Full proposal pipeline live: `extract_compliance_matrix` → `build_proposal_structure` → `scan_proposal_compliance` → `referee_proposal_compliance`.
- Recompete matching (`match_recompete_sow`), SOW extraction (`extract_statement_of_work`), event-series catalog (`get_federal_event_series`), and SBA goaling (`get_sba_goaling_share`) shipped.

> The live server is the authoritative tool list. This changelog summarizes; it does not replace live discovery.
